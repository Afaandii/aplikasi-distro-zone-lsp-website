package bootstrap

// Routes are now registered by each module via RegisterRoutes().
// This file is kept for backward compatibility but the routing
// is done in dependency.go → App.RegisterAllRoutes().
